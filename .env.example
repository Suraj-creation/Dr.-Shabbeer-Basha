# =====================================================
# Server Configuration
# =====================================================
PORT=5000
NODE_ENV=development

# =====================================================
# MongoDB Atlas Configuration (REQUIRED)
# =====================================================
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
# 
# IMPORTANT for Vercel Deployment:
# 1. Use MongoDB Atlas (cloud-hosted MongoDB)
# 2. Whitelist IP 0.0.0.0/0 in Network Access for serverless
# 3. Use a strong password without special characters that need URL encoding
# 4. Add this to Vercel Environment Variables
#
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dl-course-platform?retryWrites=true&w=majority

# =====================================================
# JWT Authentication (REQUIRED - Change in Production!)
# =====================================================
# Generate a strong secret: openssl rand -base64 64
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_use_at_least_32_characters

# JWT Token Expiry (optional, default: 7d)
JWT_EXPIRES_IN=7d

# =====================================================
# File Upload Configuration
# =====================================================
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# =====================================================
# Admin Default Credentials (Change after first login!)
# =====================================================
# IMPORTANT: Use strong, unique credentials
# Email: Use an institutional email for the course instructor
# Password: Use at least 12 characters with uppercase, lowercase, numbers, and symbols
# 
# Example format (DO NOT use these exact values):
ADMIN_EMAIL=instructor@university.edu
ADMIN_PASSWORD=YourStr0ng!P@ssw0rd

# =====================================================
# CORS Configuration
# =====================================================
# For development:
CLIENT_URL=http://localhost:3000

# For production (Vercel), update to your actual domain:
# CLIENT_URL=https://your-app.vercel.app

# =====================================================
# Vercel Specific (Auto-set by Vercel)
# =====================================================
# VERCEL=1
# VERCEL_ENV=production
